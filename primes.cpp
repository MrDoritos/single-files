#include <stdlib.h>
#include <stdio.h>
#include <map>
#include <algorithm>
#include <cmath>

template<typename T>
inline bool is_prime(T v) {
    T lim = sqrt(v);
    for (size_t i = 2; i < lim; i++) {
        if (mod())
    }
}

int main() {
    using T = int;

    T input = 0;
    std::map<T, bool> output;

    T lim = sqrt(input);

    for (T i = 0; i < lim; i++) {

    }
}